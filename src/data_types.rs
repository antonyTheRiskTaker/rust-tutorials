fn main() {
    // Floating-point numbers
    // let floating_point: f32 = 10.9;
    // println!("{}", floating_point);

    // Boolean values
    // let true_or_false: bool = true;
    // println!("{}", true_or_false);

    // Characters
    // let letter: char = 'a';
    // println!("{}", letter);

    // Tuples
    // let mut tup: (i32, bool, char) = (1, true, 's');
    // let tup2: (i8, bool, char) = (1, true, 's');
    // tup = (10, false, 'a');
    // tup.0 = 10;
    // println!("{}", tup.0);

    // Arrays
    // "[i32; 5]" means the array is set to hold only 5 elements of type int32.
    // let mut arr: [i32; 5] = []; // Error!
    // arr[4] = 3;
    // println!("{}", arr[4]);

    let x: u8 = 4;
    let y: i32 = 4;
    println!("{}, {}", x, y);

}